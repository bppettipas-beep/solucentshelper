[build]
builder = "nixpacks"

[deploy]
startCommand = "node deploy.js && node --experimental-sqlite index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[mounts]]
mountPath = "/app/data"
